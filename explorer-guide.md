# Playwright 探索规则

第一版默认使用 `Playwright + Node.js` 做确定性浏览器探索，Agent/LLM 只基于证据包归并、总结和写作。

## 登录与会话

1. 从本地配置读取浑天临时 token。
2. 使用 `getUserLoginInfo?token=...` 确认用户信息。
3. 打开目标系统测试环境地址。
4. 如果出现登录页、401/403、用户信息为空或跳转统一登录，先判定为会话异常。
5. token 过期时暂停该系统任务，等待替换 token 后从证据包断点继续。

禁止把 token 写入白皮书、截图说明和可交付日志摘要。

## 页面加载

- 等待网络空闲、关键容器出现或页面标题稳定后再采集。
- 对 SPA 页面同时记录 URL、前端路由、页面标题和主内容区域。
- 对慢页面使用短间隔多次检测，不用一次性长等待。
- 页面加载失败必须截图并记录失败原因。

## 菜单遍历

- 递归展开侧边栏、顶部菜单、折叠菜单。
- 对 hover 才出现的菜单执行 hover 后采集。
- 点击父级菜单后等待子菜单加载。
- 记录每个菜单的路径、标题、URL、访问状态和失败原因。
- 重复路由只采集一次，但保留别名菜单路径。

## 页面元素识别

优先识别这些区域：

- 查询区
- 列表区
- 工具栏
- 新增/编辑/详情弹窗
- 抽屉
- Tab 页
- 步骤条
- 导入导出入口
- 审批/提交/删除/发布等高影响按钮

对 modal、drawer、dialog、popover、tab、stepper 等交互容器要打开后采集。

## 表格和列表

- 至少采集第一页结构、查询条件、列表列、行操作和批量操作。
- 记录分页、排序、筛选、导入、导出能力。
- 不需要逐页抓取全部数据。
- 不修改或删除列表中已有数据。

## iframe 和异常页面

- 发现 iframe 时记录 iframe 来源和可访问性。
- 无法进入 iframe 时标记阻断，不静默跳过。
- 空白页、纯跳转页、重复页面、无业务信息页可以排除出核心覆盖率，但必须写明排除原因。

## 截图策略

- 模块首页或列表页截图。
- 核心功能操作截图。
- 高影响动作确认弹窗截图。
- 成功、失败、权限不足、校验不通过截图。
- 重复截图只进证据索引，不进正文。

截图命名建议：`{模块}_{功能}_{步骤}_{时间戳}.png`。

## 复跑规则

遇到以下情况时，不直接判定功能不可用：

- 缺少客户、项目、字典、审批流等前置配置。
- 表单需要业务字段但没有可选数据。
- token 过期或会话失效。
- 页面组件需要定向探索规则。

先记录阻断点，补齐条件后只复跑受影响模块或流程。
