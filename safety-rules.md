# 测试数据与写操作安全规则

## 核心原则

自动化可以在测试环境验证新增、编辑、提交、审批、删除等功能，但只能操作本轮自动化创建并登记的 `AI_AUTO_TEST_` 数据。禁止修改、提交、审批、删除、覆盖或发布系统已有数据。

## 数据来源规则

- 可以读取系统已有配置数据，例如字典项、组织、角色、审批流、客户、项目、合同类型。
- 优先复用明显安全的测试配置项。
- 缺少前置数据时，才创建 `AI_AUTO_TEST_` 前缀测试配置。
- 自动化创建的每条数据必须写入 `test-data-ledger.json`。

## 写操作前置检查

执行编辑、提交、审批、删除、发布、覆盖等动作前，必须同时满足：

1. 目标记录名称、编号或唯一标识带 `AI_AUTO_TEST_` 前缀。
2. 目标记录存在于 `test-data-ledger.json`。
3. 当前页面搜索或详情信息能证明目标记录就是该测试数据。
4. 操作日志记录本次安全检查结果。

任一条件不满足时，不执行最终写操作。

## 删除保护

删除动作必须按以下顺序执行：

1. 从 `test-data-ledger.json` 读取目标测试数据。
2. 在页面中通过唯一字段搜索或定位该测试数据。
3. 截图记录删除入口。
4. 点击删除入口并截图记录确认弹窗。
5. 再次确认目标名称或编号。
6. 仅在确认匹配时点击最终确认。
7. 删除后再次搜索，截图记录无结果或状态变化。
8. 更新测试数据清单的清理状态。

无法确认来源时，只记录删除入口和确认弹窗，不点击最终确认。

## 审批与发布保护

- 审批、发布、启用、归档、覆盖等高影响动作只允许作用于 AI 创建的数据。
- 如果动作可能触发外部通知、短信、邮件、结算、真实接口推送，默认禁止执行，记录为待确认。
- 如果页面文案提示“不可撤销”“影响已有数据”“同步外部系统”，默认禁止执行，除非用户明确授权该类测试动作。

## 白皮书表述要求

涉及高影响动作时，必须说明验证范围：

```markdown
验证说明：本次删除验证仅针对 AI 自动创建的 `AI_AUTO_TEST_` 测试数据执行，未对系统已有业务数据执行删除操作。
```

不能写成：

```markdown
系统支持删除所有合同记录。
```
