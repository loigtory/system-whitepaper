# V0 Baseline Checkpoint

## Version Target

V0 establishes execution governance before V1-V5 high-truth implementation. It does not change runtime collection, truth, narrative, database, batch, delivery, or dashboard behavior.

## Current Branch And Worktree

- Coordinator worktree: `D:/核心系统白皮书/skill/system-whitepaper`
- Coordinator branch: `codex/business-process-model`
- Coordinator commit: not committed in this checkpoint.

## Baseline Inputs

- Master plan: `docs/95-plus-truth-iteration-master-plan-2026-06-09.md`
- V1 board: `docs/采集策略重构-V1执行看板-2026-06-09.md`
- Project rules: `AGENTS.md`
- Skill rules: `SKILL.md`

## P0 Entry Risks For V1

- Formal narrative and review must be blocked when observed operation flows or workflow steps are structurally missing.
- `workflow-spec.json` must participate in the production whitepaper path and readiness lineage.
- ADP fresh reset validation requires explicit user authorization and valid local SIT prerequisites.
- DB metadata remains script-only and redacted; DB secrets and raw samples must not enter prompt or whitepaper content.

## P1 Entry Risks For V2-V4

- Current business process modeling contains ADP-specific semantics and must be generalized before cross-system trust claims.
- Formal whitepaper claim coverage should converge to 95% rather than the current lower default coverage used by fact-check.
- Golden Eval must remain isolated as evaluation-only input.

## Quality Gate Baseline

- Planned command for docs/governance changes: `npm run test:gate:auto -- --dry-run`
- If auto gate selects core: run `npm run test:gate:core`
- Full gate is reserved for collector, pipeline, DB, readiness, delivery, batch, auth, and finalization behavior changes.

## Completed Work

- Added master plan: `docs/95-plus-truth-iteration-master-plan-2026-06-09.md`
- Added V0 execution plan: `docs/superpowers/plans/2026-06-09-v0-execution-governance.md`
- Added version execution template: `docs/VERSION-EXECUTION-TEMPLATE.md`
- Added checkpoint guide: `docs/checkpoints/README.md`
- Added this baseline checkpoint: `docs/checkpoints/2026-06-09-v0-baseline.md`
- Replaced `.agents/4-agent-plan.json` with the V0-scoped 4-agent declaration.

## Worker Handoffs

No worker agents were used. V0 was executed inline because it only touched governance documentation and the agent isolation declaration. The V0 agent plan is ready for future worker-based execution, but no worktrees were created.

## Verification

- `rg -n "TBD|TODO|FIXME|marker-text" docs/VERSION-EXECUTION-TEMPLATE.md`: exited 1, no placeholder matches.
- `git -c safe.directory='D:/核心系统白皮书/skill/system-whitepaper' check-ignore -v docs/checkpoints/README.md`: exited 1, no ignore rule matched.
- `npm run agent:isolation`: exited 0. Result: V0 agent isolation passed with 4/4 workers, 3 writable workers, and 1 read-only worker.
- `npm run agent:worktrees -- --dry-run`: sandbox run blocked by `spawnSync git EPERM`; rerun outside sandbox with the same command exited 0 and reported worktree preparation ready. Dry run only; no worktrees were created.
- `rg -n "password|passwd|pwd|secret|token|cookie|JSESSIONID|HUNTIANSID|jdbc|dsn|数据库密码|客户" docs/checkpoints/2026-06-09-v0-baseline.md`: only generic safety-policy terms matched, no secret values or private connection data.
- `npm run test:gate:auto -- --dry-run`: sandbox run blocked by `spawnSync git EPERM`; rerun outside sandbox exited 0 and selected `npm run test:gate:core` with `changed=7`, `level=core`, `reasons=core-source`.
- `npm run test:gate:core`: sandbox run failed because child process spawning was blocked; minimum diagnostic `node -e "... spawnSync(process.execPath, ['--version']) ..."` returned `spawnSync C:\Program Files\nodejs\node.exe EPERM`. Rerun outside sandbox exited 0. Result: `npm test` passed 396/396, `npm run pack:check` passed, and `npm run agent:isolation` passed.

## Residual Risk

- V0 did not run real ADP/SIT collection, browser exploration, DB profiling, batch acceptance, delivery readiness, or final truth readiness because the version only establishes execution governance.
- `.agents/4-agent-plan.json` declares future worker ownership but worktrees were not created because V0 was intentionally executed by the coordinator.

## Environment And Data Safety

V0 implementation did not touch real systems, browser sessions, DB metadata, secrets, cookies, business runtime outputs, or customer artifacts. Verification used standard local test/package cache paths under ignored directories such as `.tmp` and `.npm-cache`.

## Next Step

After V0 governance is accepted, start V1: flow evidence hard gate.
