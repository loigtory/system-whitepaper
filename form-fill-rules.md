# 表单填充与前置配置规则

## 总原则

表单填写不是随便编数据。优先读取页面已有配置和业务可选项；只有缺少可用前置条件时，才创建 `AI_AUTO_TEST_` 测试配置。

## 字段填充优先级

1. 使用页面已有测试配置项，例如测试客户、测试项目、测试审批流。
2. 使用页面现有可选项，例如字典、组织、角色、合同类型。
3. 创建 `AI_AUTO_TEST_` 前缀的前置配置。
4. 无法确认业务含义或规则时，停止提交并记录待确认项。

## 常见字段规则

| 字段类型 | 填充规则 |
| --- | --- |
| 名称/标题 | `AI_AUTO_TEST_[业务对象]_[时间戳]` |
| 编号 | `AIAT[时间戳]` 或页面自动生成 |
| 金额 | 小额正数，例如 `100` |
| 数量 | 小整数，例如 `1` |
| 日期 | 当前日期或未来日期，按页面校验选择 |
| 手机号 | 测试号段，例如 `13800000000` |
| 邮箱 | `aitest@example.com` |
| 备注 | `AI 自动化测试数据，仅用于系统白皮书验证` |
| 下拉框 | 优先选择非禁用、非占位的测试项或第一个有效项 |
| 树形选择 | 优先选择测试节点；没有时选择可用叶子节点 |
| 文件上传 | 使用预置测试文件，并记录文件名 |

## 复杂组件处理

- 级联下拉：按页面已有选项逐级选择，不跳级猜测。
- 组织树/人员树：优先选择测试组织或当前用户可见的安全节点。
- 富文本：填写短测试说明，不粘贴复杂 HTML。
- 日期范围：选择合法短范围。
- 步骤表单：每步截图并记录必填阻断。
- 动态表格：只新增最小可提交行。

## 前置配置识别

如果提交失败或字段无可选项，判断是否缺少前置配置：

- 客户/供应商/项目/合同类型
- 组织/部门/人员
- 字典/分类/标签
- 审批流/流程模板
- 产品/物料/费用类型

缺少前置配置时，先记录阻断点；能安全创建的，创建 `AI_AUTO_TEST_` 配置后定向复跑；不能创建的，进入待确认事项。

## 提交前检查

提交前必须确认：

- 目标数据是本轮自动化创建的数据。
- 表单不会覆盖系统已有数据。
- 页面没有提示外部通知、真实结算、推送生产外部系统。
- 高风险动作符合 `safety-rules.md`。

否则不提交，只记录表单结构和阻断原因。
